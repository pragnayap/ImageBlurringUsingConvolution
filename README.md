# ImageBlurringUsingConvolution
The primary objective of this project was to build a simple image blurring model. The fundamental approach treats a digital image as a numerical matrix and applies convolution-based filtering to produce a blurred output.  The implementation is purely NumPy-based with Matplotlib for visualization.
